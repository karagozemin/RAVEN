# RAVEN — core runtime dependencies
# Python 3.11+ recommended

# Async HTTP / SSE transport for TxLINE feed
aiohttp>=3.9.0
httpx>=0.27.0

# Config
python-dotenv>=1.0.0

# CLI / terminal UI (Live Control Room comes later)
rich>=13.7.0

# Data / math (Fair-Value Engine, event-hazard model — later layers)
numpy>=1.26.0
scipy>=1.11.0

# Test suite
pytest>=8.0.0

